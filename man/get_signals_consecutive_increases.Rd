% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/consecutive_increases.R
\name{get_signals_consecutive_increases}
\alias{get_signals_consecutive_increases}
\title{Detect Five Consecutive Increases in Incidence}
\usage{
get_signals_consecutive_increases(
  data_aggregated,
  number_of_time_units = 52,
  time_unit = "weekly"
)
}
\arguments{
\item{data_aggregated}{A data frame of aggregated surveillance data containing
a numeric \code{cases} column.}
\item{number_of_time_units}{Integer specifying the number of final time units
for which alarms should be generated.}
\item{time_unit}{A character specifying the aggregation unit. One of
\code{"weekly"}, \code{"biweekly"}, or \code{"monthly"}.}
}
\value{
\code{data_aggregated} with logical \code{alarms} and numeric \code{upperbound} and
\code{expected} columns added. The latter two columns are \code{NA} because this
rule-based method does not estimate a baseline.
}
\description{
Flags an alarm when incidence has increased strictly in each of the five
preceding transitions. An alarm therefore requires six consecutive
observations. Ties and decreases reset the run of increases.
}
\details{
Only the final \code{number_of_time_units} observations are monitored. Earlier
observations establish whether a run was in progress when monitoring starts.
}
\examples{
data <- data.frame(
  year = rep(2024, 7), week = 1:7,
  cases = c(1, 2, 3, 4, 5, 6, 4)
)
get_signals_consecutive_increases(data, number_of_time_units = 7)
}
